extern crate rand;

mod part1;
mod part2;

fn main() {
    // uncomment individually to run part (both are endless loops, so you will
    // have to terminate the program in order to quit it)
    //part1::run();
    //part2::run();
    println!("ERROR: Modify main.rs in order to run part 1 OR part 2 !!!");
}
